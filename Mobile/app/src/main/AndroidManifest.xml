<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".core.DThoughtsApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dthoughts"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".post.FeedActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".post.CreatePostActivity" />
        <activity android:name=".post.PostDetailActivity" />
        <activity android:name=".post.GuestFeedActivity" />
        <activity android:name=".auth.RegisterActivity" />
        <activity android:name=".auth.ForgotPasswordActivity" />
        <activity
            android:name=".auth.ResetPasswordActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="dthoughts.com"
                    android:pathPrefix="/reset-password"
                    android:scheme="http" />
                <data
                    android:host="dthoughts.com"
                    android:pathPrefix="/reset-password"
                    android:scheme="https" />
            </intent-filter>
        </activity>
        <activity android:name=".profile.ProfileActivity" />
        <activity android:name=".profile.EditProfileActivity" />
        <activity android:name=".notification.NotificationsActivity" />
        <activity android:name=".draft.DraftsActivity" />
    </application>

</manifest>